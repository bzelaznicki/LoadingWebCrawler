{
  "tasks": {
    "dev": "deno run -A --watch main.ts",
    "start": "deno run -A main.ts",
    "playwright:install": "deno run --allow-run --allow-read --allow-write --allow-env --allow-net npm:playwright install"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
